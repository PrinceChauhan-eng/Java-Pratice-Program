package JavaExecrise;

import AccessModifiers.*;


public class DefaultMain {
    static void main() {
        //DefaultAccessModifiers ds =new  DefaultAccessModifiers();
        /*In this example, the program will show the compile-time error
        when we try to access a default modifier class from a different package. */
    }
}
